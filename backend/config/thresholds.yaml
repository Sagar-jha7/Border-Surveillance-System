# thresholds.yaml
# ----------------
# Override any of these values; all keys are optional — defaults are in settings.py.

detection:
  day_confidence: 0.35
  night_confidence: 0.25
  night_brightness_threshold: 60     # mean pixel brightness (0-255)
  tier2_slice_height: 320
  tier2_slice_width: 320
  tier2_overlap_ratio: 0.2
  tier3_min_contour_area: 900        # px² minimum blob to count as unidentified object motion

tracking:
  max_lost_frames: 30
  min_hits: 3

reid:
  embedding_cosine_threshold: 0.65
  gallery_ttl_seconds: 300

alerts:
  group_distance_threshold: 100.0    # pixels within which two tracks form a group
  group_velocity_threshold: 20.0
  loitering_frames: 150
  boundary_crossing_priority: RED
  group_priority: AMBER
  unidentified_priority: GRAY

pipeline:
  target_fps: 15
  frame_width: 854
  frame_height: 480
  preview_enabled: true
