# Core detection / pipeline dependencies
ultralytics>=8.2.0
opencv-python>=4.9.0
numpy>=1.26.0
supervision>=0.21.0

# Phase 2+ (backend API)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
websockets>=12.0
python-multipart>=0.0.9
pydantic>=2.0.0

# Phase 3+ (SAHI slicing, drone detection)
sahi>=0.11.19

# Phase 4+ (night mode)
# opencv-python already includes CLAHE — no extra dep

# Phase 5+ (embeddings)
insightface>=0.7.3
onnxruntime>=1.18.0         # required by insightface
torchreid>=1.4.0

# Utilities
PyYAML>=6.0
Pillow>=10.0.0
